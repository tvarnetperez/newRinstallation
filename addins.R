# Hotkey to align assignment operators
remotes::install_github("seasmith/AlignAssign")

# To fiddle vectors, auto-toggle quotes and facilitate pasting from the web
install.packages(
   "datapasta",
   repos = c(mm = "https://milesmcbain.r-universe.dev", getOption("repos")))
